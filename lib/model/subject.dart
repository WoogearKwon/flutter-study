import 'package:flutter/material.dart';

class Subject {
  final String name;
  final String description;
  final Widget icon;
  final String route;

  Subject(this.name, this.description, this.icon, this.route);
}